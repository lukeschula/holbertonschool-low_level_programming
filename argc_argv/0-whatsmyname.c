#include <stdio.h>
/**
 * main - hat prints its name, followed by a new line.
 * @argc: argument count 
 * @argv: argument vector
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);
	{
		return (0);
	}
}
